rootProject.name = 'FlowableDataExposer'

// New modules: core (library) and web (Spring Boot app + process resources)
// Added complexSample as a second web module
include 'core', 'web', 'complexSample'