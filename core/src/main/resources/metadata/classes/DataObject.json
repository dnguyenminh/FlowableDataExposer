{
  "$schema": "/metadata/class-schema.json",
  "class": "DataObject",
  "parent": "FlowableObject",
  "description": "Shared data / nested objects (inherits audit from FlowableObject)",
  "jsonPath": "$",
  "fields": [
    { "name": "id", "jsonPath": "$.id", "type": "string" },
    { "name": "type", "jsonPath": "$.type", "type": "string" }
  ]
}
